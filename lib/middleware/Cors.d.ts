export declare const Cors: {
    /**
     * 加载Cors
     * @param app
     * @param server
     * @param config
     * @param enabled
     */
    init: (app: any, server: any, config?: {}, enabled?: boolean) => void;
};
