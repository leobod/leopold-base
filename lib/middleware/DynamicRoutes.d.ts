export declare const DynamicRoutes: {
    /**
     * 加载动态路由
     * @param app
     * @param server
     * @param config
     * @param enabled
     */
    init: (app: any, server: any, config?: {
        routes: never[];
    }, enabled?: boolean) => void;
};
