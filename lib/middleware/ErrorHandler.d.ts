export declare const ErrorHandler: {
    /**
     * 加载默认错误处理
     * @param app
     * @param server
     * @param config
     * @param enabled
     */
    init(app: any, server: any, config?: {}, enabled?: boolean): void;
};
